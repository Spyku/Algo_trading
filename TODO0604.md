# TODO 0604 — cross-laptop verification of the data-lag / determinism fixes

Created 2026-06-04 (DESKTOP). Goal: re-run the key checks on the **other laptop** to
verify (a) the fixes reproduce and (b) whether GPU LightGBM is **cross-machine**
deterministic — which decides whether the sanity check should use GPU or CPU.

---

## What was fixed Jun 3–4 (context — already shipped & pushed)

1. **Closed-bar inference** — live trader infers on the last *closed* hourly bar, not the
   forming candle. Signature: `inference_row_age_h = 1.0` in `output/cycle_metrics.csv`.
2. **Per-cycle feature log** — `output/inference_snapshots.jsonl` (exact features + probas).
3. **Daily-merge lag mirrored into the ED engine** (`crypto_trading_system_ed.py`):
   `DAILY_MERGE_LAG_DAYS=1`, `ONCHAIN_MERGE_LAG_DAYS=2`. The ED engine (live trader +
   validator) now matches FAYE training — no daily-feed look-ahead. Verify with
   `python tools/audit_feature_lag.py` (0 violations = clean).
4. **Validator** (`tools/validate_core_against_signal_log.py`): config-aware buckets,
   asymmetric min_confidence gate (BUY gated, SELL not), closed-bar realignment.
5. **Grid-CSV save hardened** against Drive-sync dir-hiding (`crypto_trading_system_faye.py`).

**Proven on DESKTOP:** the model is **deterministic** (RF+GPU-LGBM bit-identical across two
fits). Post-fix sanity = **86.7% GPU / 83.3% CPU**. The residual is NOT the leak and NOT
noise — it's **training-window data revision** (derivatives/OI backfill changes the recent
training rows) + the **device mismatch** (sanity forces CPU LGBM, live uses GPU).

---

## DO ON LAPTOP-B

```
# 0. sync the fixes
git pull            # (and let Google Drive finish syncing data/ if you use the Drive copy)

# 1. confirm the lag is present + clean
python tools/audit_feature_lag.py --asset ETH --horizon 5     # expect: 0 VIOLATIONS

# 2. CROSS-MACHINE DETERMINISM PROBE  <-- the main point
python tools/check_proba_repro.py "2026-06-04 04:00:00"
#   run1 must == run2  (deterministic on this machine)
#   compare to DESKTOP reference:  RF=0.51463784  LGBM=0.05573897
#     - RF equal + LGBM equal  -> GPU is cross-machine deterministic
#     - RF equal + LGBM differs -> GPU differs across GPUs (the --cpu-lgbm reason)
#     - RF differs             -> data/ not identical between machines (sync lag) — re-pull
```

### Decision this informs
- **If GPU is cross-machine deterministic** → drop `--cpu-lgbm` from the 8:30 sanity
  (`tools/sanity_check.py` line ~60) so the validator matches the GPU live trader. Expected
  to push the post-fix bucket toward the high-90s.
- **If GPU differs across GPUs** → keep `--cpu-lgbm` for portable/deterministic validation,
  and accept it will never exactly equal the GPU live trader (document the known gap).

---

## STILL PENDING (not yet implemented — do after the laptop-B probe decides device)

- [ ] Sanity device decision: GPU (match live) vs CPU (portable) — per laptop-B result above.
- [ ] Add `--min-age-h` filter to the validator so it scores only **settled** hours
      (training window no longer revising, ≥ ~36–48h old). The clean ~100% read needs this.
      NOTE: no settled *post-lag-fix* entries exist until ~Jun 5–6 (lag fix went live
      2026-06-03 23:27 UTC; trader restarted then).
- [ ] Re-run the 4-month FAYE HRST to completion (crashed on a Drive blink at 6h grid save;
      now hardened) → then compare 4-month vs 2-month regime winners.
- [ ] Re-run the earlier 1mo/2mo ED backtests (`bt_new_vs_old_bear.py`, `bt_am1_vs_B_regime.py`)
      — they were run on the **un-lagged** ED engine (leaky); redo now that ED is lagged.

---

## Watch-outs
- The live trader's LGBM factory is `device='gpu'` (`crypto_trading_system_ed.py:2035/2048`).
- `signal_log` is UTC; `cycle_metrics.csv` is local CEST (+2h).
- Don't truncate live `data/` files (the running trader reads them); use a copied snapshot
  (`tools/build_data_snapshot.py YYYY-MM-DD`) for past-anchored runs, and add `--no-data-update`
  so the HRST doesn't re-download fresh data into the snapshot.
