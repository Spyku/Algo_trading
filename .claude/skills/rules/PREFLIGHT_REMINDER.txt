[PRE-FLIGHT] Alex's rules -- run BEFORE acting, on EVERY message (no exceptions, incl. build/quick tasks):
1. VERIFY with numbers from the source (code file:line / prod CSV / regime_config / logs / data) -- never gut or memory. Say "unverified" if you can't.
1b. DIAGNOSING *why* something happens? A root cause is a HYPOTHESIS until a test RULES OUT the alternatives -- list >=2 causes, run the test that could prove your favorite WRONG, state confidence + what would change your mind. A number that merely FITS your guess is not proof (confirmation bias). Test-to-break BEFORE asserting; never present a cause as settled while an untested alternative remains.
2. CLASSIFY the request -> fire the matching rule family (F1 promotion, F2 inference, F3 execution, F4 data, F5 process, F6 research; forks = F1+F6+wall) -> STOP if a gate fails.
3. NEW IDEA? prior-art check FIRST -- grep ARCHIVED_LOG.md (C01-C88 scoreboard) + TODO.md + CLAUDE.md + README.md; if already DEAD/SHELVED/done, report the prior verdict (+revival conditions) instead of redoing it.
4. FORBIDDEN -- do NOT, without explicit OK: infect production from a research/fork path (Chinese wall); monkey-patch; flag/edit live trading_config without asking; promote mid-position; trade an enabled:false asset; judge a feature by importance/ungated screen (gated sim only); run HRST on a new asset/horizon/timeframe without discovering PySR for THAT timeframe first; add embargo in live; auto-launch long/expensive or irreversible actions without asking; pursue a new idea without the prior-art check; cp a fresh mock (use the one canonical crypto_trading_system_faye_mock.py).
5. DONE? a VALIDATED code change must be finished: update README + CLAUDE.md (+TODO/ARCHIVED_LOG) then commit + push (authorized for validated changes; never commit config/ secrets).
Load the focused skill for the task: /promote-check (live writes) | /inference-change (live signal path) | /data-source (new raw feed) | /add-feature (new computed feature) | /fork-engine (engine fork or new asset/horizon/timeframe) | /validate-research (judging or proposing an idea). Master index + FORBIDDEN list: /rules
