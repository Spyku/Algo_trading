## ⚡ TODO — Active work only

> **📌 LIVE STATE (2026-07-01):** ETH `sma168>sma480` · **bull 6h@80 / bear 4h@65** · shields OFF · single-form rally-cooldown (bull rr8≥2.5 cd6 / bear rr8≥2.5 cd8) · min_sell 0% · max_hold 10h · maker ON. **Promoted 2026-06-30 00:32** — backups `config/regime_config_ed_pre_HRST_20260630_0032.json` + `models/crypto_ed_production_pre_HRST_20260630_0032.csv`; **rollback** = `cp` both back. Trader live + healthy, shadow `live==core` 100%. Per-asset: **ETH live; BTC/SOL/LINK `enabled:false`; BNB code-wired (PySR/HRST not run); XRP removed.**
>
> **🟡 DATA RECOVERY (low urgency, standalone):** orderbook+IV history is still clobbered (orderbook 34 / iv 48 rows). RESTORE from `data/_v2_snapshot_ablate58_20260614_135510/macro_data/` (orderbook 1350 / iv 2358 rows) → `_merge_preserve_history` the live files (on the DESKTOP, avoid a Drive conflict) so a FUTURE HRST can re-select the enabled `ob_imbalance`. Then ADD orderbook/iv to `_INTEGRITY_REGISTRY`. **No restart-timing constraint** — the `_atomic_to_csv` shrink-guard now protects the file. *(The G1/G2 sparse-feature A/B already RAN 2026-06-22 on the pre-clobber full history; clobber class RESOLVED — see ARCHIVED_LOG.)*

**Companion file**: [ARCHIVED_LOG.md](ARCHIVED_LOG.md) — closed verdicts + canonical scoreboard C01-C88 (prior-art check here before any new idea).

## 📊 At-a-glance — active dashboard (2026-07-01)

| Pri | Item | Where | Status |
|---|---|---|---|
| 🚀 **P3 — CPCV retest (lowered from P1 2026-07-02, low EV)** | Deprioritized after the methodology reckoning: **"is CPCV better?" is only measurable OOS** (CPCV-selected vs current-selected config on a held-out window) = the full ~20-35h CPCV HRST — **already run 2026-05-11 → NEUTRAL** (no OOS re-rank vs APF-Optuna, [ARCHIVED_LOG.md:1606](ARCHIVED_LOG.md#L1606)). The leak-fix re-run is very likely neutral again — the leak was **symmetric** (C01 lesson), so return/selection comparisons don't flip. Only the cheap **PBO overfit-diagnostic** remains (~1-3h: `tools/cpcv_pbo_faye.py` = wrap faye's leakage-free per-fold eval with the 15-path combinatorial-purged split; 3 CPCV fns in `ARCHIVED/2026-05-30_post_faye_cleanup/variants/crypto_trading_system_ed_cpcv.py` — `_build_cpcv_paths`/`_compute_pbo`/`_cpcv_evaluate_one_candidate`) — but that measures "are the live winners overfit?", **not** "is CPCV better." Run only for overfit intel, not as a selection upgrade. | Desktop | 🚀 **P3 — likely-neutral, diagnostic-only** |
| ⚪ **P4 — Log housekeeping (leftover)** | The P0-0613 **code-change is DONE** (verified 2026-07-01): faye.py:305/311 + ed.py:142/147 have the top-level `import re` guard + route engine logs to `logs/hrst/` (102 files); no empty-turds. **Leftover (cosmetic, ~0 value):** trader still tees to flat `logs/ed_runtime_*.log` ([crypto_live_trader_ed.py:124](crypto_live_trader_ed.py#L124)) not `logs/trader/`; ~1275 old flat `*.log` never migrated into subdirs; `logs/misc/` unused. | Laptop | ⚪ **optional** |
| 🚀 **P3 — BTC directional retry (downgraded 2026-07-01, low EV)** | **Deprioritized — BTC's backtests don't survive forward, and a win adds little.** BTC *backtests* fine (an HRS scored +36.15% / 70% WR, [ARCHIVED_LOG.md:4318](ARCHIVED_LOG.md#L4318); early +50% was pre-embargo/inflated) BUT the one FORWARD/OOS test **FAILED** — the 2026-04-06 disable (45% WR, avg loss > avg win on 1m OOS). Low strategic value even if it worked: ~0.8 corr to the ETH sleeve (depth, not breadth) + a fee-clearing headwind (BTC 32.5% vs ETH 52.3% of bars clear the fee). Mode P done (06-12, `models_faye/pysr_BTC_*.json`); a leakage-free `HRST BTC 5,6,7,8h` *could* re-settle the 04-06 disable, but carry / vol-targeting are the real orthogonal adds. | Desktop | 🚀 **P3 — low prior, backlog** |
| 🟦 **FRONTIER — Portfolio layer** | Risk-budget allocator (rolling Ledoit-Wolf + ERC) + book-level DD governor across the directional + carry sleeves (on equity curves, NOT overriding SELL timing). Book Sharpe ≈ IC·√(independent bets); carry pays only if sized by RISK contribution. The strategic destination. | — | 🔒 **GATED on carry live** (C83, paper) |
| 🔥 **P0 — Shadow match-rate monitor** | Primary live-correctness gate — every 1-2 days check `shadow_signal_diff.csv`; any drop below ~99% = a NEW bug to investigate. | Desktop (passive) | 🕐 **ongoing** |
| 🚀 **P3 — Continuous macro archeology** | Nightly `python tools/drive_archeology.py --preset all` so future PIT validation has bounded drift. | Desktop (cron) | 📅 **setup** |
| 🚀 **P3 — Research backlog** | C14 vol-conditional triple-barrier retest · C11 VPIN 5-min · C15 meta-label SOL/BTC (blocked, assets shelved) · IDEA QUEUE Tier A (C13-narrow, C54, C55, C58, C59) · Tier B (C60-C82, 16/23 patcher-ready) · 6h-winner trade-count check · 8h Mode-D survivor-count · feature-importance stability after cache fix · ffill-vs-mean_last_10 counterfactual. **Prior-art check ARCHIVED_LOG C01-C88 before picking any.** | research backlog | 📋 **untested** |
| ⚪ **P4** | Kalshi prediction-market integration (needs API key). | backlog | ⚪ |

*Closed/stale rows (the whole 2026-05→06 arc: backtest≠live, data-clobber, Mode-V deadlock, shadow-down, Mode-C, gate-simplicity, GB/LGBM, embargo 1-4h, May-31 config watches, SESSION 0620) archived 2026-07-01 → [ARCHIVED_LOG.md](ARCHIVED_LOG.md). Full pre-cleanup TODO in `TODO_pre_cleanup_20260701.md` + git.*
